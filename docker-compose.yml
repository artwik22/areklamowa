version: '3.8'

services:
  frontend:
    # Build z Git repository
    build:
      context: https://github.com/TWOJE_USERNAME/TWOJE_REPO.git#main
      dockerfile: Dockerfile
    
    container_name: areklamowa-prod

    # Mapowanie portów: 8080 na Twoim serwerze kieruje do portu 80 w kontenerze
    ports:
      - "8080:80" 
      
    restart: unless-stopped
